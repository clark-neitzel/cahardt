# Backups e arquivos temporários de editor
*.orig
*.bak
*.swp
*~

# Sistema
.DS_Store
Thumbs.db

# Dependências (caso escapem dos .gitignore por pasta)
node_modules/

# Logs
*.log
npm-debug.log*

# Segredos locais (NUNCA versionar)
.admin-secret
backend/scripts/.admin-secret

# Variáveis de ambiente (só o .env.example é versionado, como template)
.env
.env.*
!.env.example

# Protótipo Kit Festa (referência local, não versionar)
/Pedidos Kit Festa/
# Protótipo Congelados (referência local, não versionar)
/Página Hardt Congelados/
backend/backups/
